org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
kotlin.code.style=official
android.useAndroidX=true

# --- Maven Central Publishing ---
# Set these in ~/.gradle/gradle.properties or pass via -P flags / env vars.
#
# Sonatype OSSRH credentials (or set OSSRH_USERNAME / OSSRH_PASSWORD env vars):
#   ossrhUsername=
#   ossrhPassword=
#
# GPG signing (or set GPG_SIGNING_KEY / GPG_SIGNING_PASSWORD env vars):
#   signingKey=<ASCII-armored GPG private key>
#   signingPassword=<passphrase>
